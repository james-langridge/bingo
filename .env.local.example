# Upstash Redis credentials
# When using Vercel's Upstash integration, these are automatically added
# For local development, get these from https://console.upstash.com/

KV_REST_API_URL=your_upstash_redis_rest_url_here
KV_REST_API_TOKEN=your_upstash_redis_rest_token_here

# Optional read-only token (not required for this app)
# KV_REST_API_READ_ONLY_TOKEN=your_read_only_token_here

# Copy this file to .env.local and fill in your actual values
# DO NOT commit .env.local to version control