# Upstash Redis (for data storage)
KV_REST_API_URL=https://your-upstash-url.upstash.io
KV_REST_API_TOKEN=your-upstash-token

# For Railway/Fly deployment (Redis for pub/sub)
REDIS_URL=redis://default:password@redis-host:6379

# Server configuration
PORT=3001
HOST=0.0.0.0
NODE_ENV=production
LOG_LEVEL=info

# Client configuration (optional - defaults to same origin)
# VITE_API_URL=https://your-server.railway.app